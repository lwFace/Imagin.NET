<i:BaseWindow
    x:Class="Desktop.OptionsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:i="http://imagin.tech/imagin/common"
    Height="{i:Options OptionsWindowHeight, Mode=TwoWay}"
    ResizeMode="CanResizeWithGrip"
    Title="{i:Loc Options}"
    TitleIcon="{i:Image Desktop, Images/Options.png}"
    Width="{i:Options OptionsWindowWidth, Mode=TwoWay}"
    WindowStartupLocation="CenterScreen"
    WindowStyle="ToolWindow">
    <i:PropertyGrid
        BorderThickness="0"
        DescriptionVisibility="Collapsed"
        GroupName="Category"
        GroupVisibility="True"
        HeaderVisibility="Collapsed"
        HeadersVisibility="None"
        Source="{i:Options}"
        TypeVisibility="Collapsed"/>
</i:BaseWindow>